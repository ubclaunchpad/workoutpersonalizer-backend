import { Express } from 'express';

describe('Unit tests for FilterController', function () {
  let app: Express;

  beforeAll(async () => {
    // TODO: setup for mock database
  });

  afterAll(async () => {
    // TODO: setup for mock database
  });

  beforeEach(async () => {
    // TODO: setup for mock database
  });

  test('GET /filters/muscleGroups - get values of all muscle groups', async () => {
    // TODO: test case
  });
});
